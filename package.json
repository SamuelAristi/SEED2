{
  "name": "seed-automatizando-campo",
  "version": "1.0.0",
  "description": "Sistema inteligente para control de cultivos de almacigo",
  "main": "backend/index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install-all && echo '✅ Setup completed! Run npm run dev to start development servers'",
    "test": "cd backend && npm test",
    "lint": "cd frontend && npm run lint"
  },
  "keywords": ["agriculture", "iot", "monitoring", "crops", "react", "express", "supabase"],
  "author": "SEED Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
